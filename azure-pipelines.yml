trigger:
  branches:
    include:
      - '*'

pool:
  vmImage: 'ubuntu-latest'

variables:
  - group: build.variables
  - name: MAVEN_CACHE_FOLDER
    value: $(Pipeline.Workspace)/.m2/repository
  - name: APP_NAME
    value: $(projectName)
  - name: RUNTIME_VERSION
    value: '4.9.0'
  - name: CH2_TARGET
    value: 'Cloudhub-US-East-2'
  - name: CH2_ENVIRONMENT
    value: 'Sandbox'
  - name: CH2_REPLICAS
    value: '1'
  - name: CH2_VCORES
    value: '0.1'

stages:
  #####################################################
  # BUILD STAGE
  #####################################################
  - stage: Build
    displayName: 'Build Mule Application'
    jobs:
      - job: BuildJob
        displayName: 'Maven Build'
        steps:
          - task: DownloadSecureFile@1
            name: mavenSettings
            displayName: 'Download Maven Settings'
            inputs:
              secureFile: 'settings.xml'
          
          - script: |
              echo "📦 Installing Maven settings..."
              mkdir -p ~/.m2
              cp $(mavenSettings.secureFilePath) ~/.m2/settings.xml
              echo "✅ Maven settings installed"
            displayName: 'Install Maven Settings'
          
          - task: JavaToolInstaller@0
            displayName: 'Setup Java 17'
            inputs:
              versionSpec: '17'
              jdkArchitectureOption: 'x64'
              jdkSourceOption: 'PreInstalled'
          
          # - task: Cache@2
          #   displayName: 'Cache Maven Dependencies'
          #   inputs:
          #     key: 'maven | "$(Agent.OS)" | **/pom.xml'
          #     restoreKeys: |
          #       maven | "$(Agent.OS)"
                maven
              path: $(MAVEN_CACHE_FOLDER)
          
          - script: |
              echo "=========================================="
              echo " Building Mule Application"
              echo "=========================================="
              echo "Project: $(APP_NAME)"
              echo "Runtime: $(RUNTIME_VERSION)"
              echo ""
              
              mvn clean package \
                -DskipTests \
                -Dsecure.key=mule \
                -Dmule.env=dev \
                -s ~/.m2/settings.xml
              
              if [ $? -eq 0 ]; then
                echo ""
                echo "✅ BUILD SUCCESSFUL"
                echo ""
                ls -lh target/*.jar
              else
                echo "❌ BUILD FAILED"
                exit 1
              fi
            displayName: 'Maven Package'
          
          - task: CopyFiles@2
            displayName: 'Copy Build Artifacts'
            inputs:
              Contents: '**/target/*.jar'
              TargetFolder: $(Build.ArtifactStagingDirectory)
              CleanTargetFolder: true
              flattenFolders: true
          
          - task: PublishPipelineArtifact@1
            displayName: 'Publish Build Artifacts'
            inputs:
              targetPath: $(Build.ArtifactStagingDirectory)
              artifact: 'mule-app'
              publishLocation: 'pipeline'

  #####################################################
  # DEPLOY STAGE - CloudHub 2.0
  #####################################################
  - stage: Deploy
    displayName: 'Deploy to CloudHub 2.0'
    dependsOn: Build
    condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/main'))
    jobs:
      - job: DeployJob
        displayName: 'CloudHub 2.0 Deployment'
        steps:
          - task: DownloadSecureFile@1
            name: mavenSettings
            displayName: 'Download Maven Settings'
            inputs:
              secureFile: 'settings.xml'
          
          - script: |
              mkdir -p ~/.m2
              cp $(mavenSettings.secureFilePath) ~/.m2/settings.xml
            displayName: 'Install Maven Settings'
          
          - task: JavaToolInstaller@0
            displayName: 'Setup Java 17'
            inputs:
              versionSpec: '17'
              jdkArchitectureOption: 'x64'
              jdkSourceOption: 'PreInstalled'
          
          - task: DownloadPipelineArtifact@2
            displayName: 'Download Build Artifacts'
            inputs:
              artifactName: 'mule-app'
              targetPath: $(System.DefaultWorkingDirectory)/target
          
          # ✅ FIXED: Use mule-maven-plugin:deploy directly instead of mvn deploy
          - script: |
              echo "=========================================="
              echo " CloudHub 2.0 Deployment"
              echo "=========================================="
              echo "Application: $(APP_NAME)"
              echo "Environment: $(CH2_ENVIRONMENT)"
              echo "Target: $(CH2_TARGET)"
              echo "Runtime: $(RUNTIME_VERSION)"
              echo "Replicas: $(CH2_REPLICAS)"
              echo "vCores: $(CH2_VCORES)"
              echo "Organization: $(muleOrg)"
              echo ""
              
              # Verify JAR exists
              JAR_FILE=$(find target -name "*.jar" -type f | head -1)
              
              if [ -z "$JAR_FILE" ]; then
                echo "❌ ERROR: JAR file not found in target directory!"
                exit 1
              fi
              
              echo "📦 JAR File: $JAR_FILE"
              echo "📦 File Size: $(du -h "$JAR_FILE" | cut -f1)"
              echo ""
              echo "🚀 Starting deployment to CloudHub 2.0..."
              echo ""
              
              # ✅ CRITICAL FIX: Use mule-maven-plugin:deploy goal directly
              # This skips the maven-deploy-plugin that tries to publish to Exchange
              mvn mule:deploy \
                -DskipTests \
                -Dmaven.deploy.skip=true \
                -Dconnectedapp.clientid=$(anypointClientId) \
                -Dconnectedapp.clientsecret=$(anypointClientSecret) \
                -Danypoint.orgid=$(muleOrgId) \
                -Dcloudhub2.applicationName=$(APP_NAME) \
                -Dcloudhub2.environment=$(CH2_ENVIRONMENT) \
                -Dcloudhub2.target=$(CH2_TARGET) \
                -Dcloudhub2.replicas=$(CH2_REPLICAS) \
                -Dcloudhub2.vcores=$(CH2_VCORES) \
                -Dmule.env=dev \
                -Dsecure.key=mule \
                -s ~/.m2/settings.xml
              
              DEPLOY_EXIT_CODE=$?
              
              echo ""
              if [ $DEPLOY_EXIT_CODE -eq 0 ]; then
                echo "=========================================="
                echo " ✅ DEPLOYMENT SUCCESSFUL!"
                echo "=========================================="
                echo ""
                echo "Application: $(APP_NAME)"
                echo "Environment: $(CH2_ENVIRONMENT)"
                echo "Target: $(CH2_TARGET)"
                echo ""
                echo "🌐 Check status in Runtime Manager:"
                echo "   https://anypoint.mulesoft.com/cloudhub"
                echo ""
              else
                echo "=========================================="
                echo " ❌ DEPLOYMENT FAILED"
                echo "=========================================="
                echo ""
                echo "Exit Code: $DEPLOY_EXIT_CODE"
                echo ""
                echo "💡 Troubleshooting:"
                echo "1. Verify Connected App permissions"
                echo "2. Check environment exists: $(CH2_ENVIRONMENT)"
                echo "3. Verify target exists: $(CH2_TARGET)"
                echo "4. Ensure org ID is correct: $(muleOrgId)"
                echo ""
                exit 1
              fi
              
            displayName: 'Deploy to CloudHub 2.0'
            env:
              anypointClientId: $(anypointClientId)
              anypointClientSecret: $(anypointClientSecret)
              muleOrg: $(muleOrg)
              muleOrgId: $(muleOrgId)
